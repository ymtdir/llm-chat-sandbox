# プロセスガイド

Git運用・テスト戦略・コードレビュー・CI/CDのガイドです。

## 基本原則

### 1. 具体例を豊富に含める

```typescript
// ✅ 良い例: 役割が明確
const userAuthentication = new UserAuthenticationService();

// ❌ 悪い例: 曖昧
const auth = new Service();
```

### 2. 理由を説明する

ルールには「なぜそうするのか」を明記します。

### 3. 測定可能な基準を設定

```
// ❌ コードカバレッジは高く保つこと
// ✅ ユニットテスト: 80%以上、統合テスト: 60%以上
```

## Git運用ルール

### ブランチ戦略

```
main (本番環境)
└── develop (開発・統合環境)
    ├── feature/* (新機能開発)
    ├── fix/* (バグ修正)
    └── release/* (リリース準備)※必要に応じて
```

**運用ルール**:

- **main**: 本番リリース済みの安定版コードのみ。タグでバージョン管理
- **develop**: 次期リリースに向けた最新の開発コード。CIで自動テスト実施
- **feature/\*, fix/\***: developから分岐し、PR経由でdevelopへマージ
- **直接コミット禁止**: すべてのブランチでPRレビューを必須
- **マージ方針**: feature→develop は squash merge、develop→main は merge commit を推奨

### コミットメッセージ規約

詳細は `../../rules/commit-conventions.md` を参照。

### プルリクエストのテンプレート

    ## 変更の種類

    - [ ] 新機能 (feat)
    - [ ] バグ修正 (fix)
    - [ ] リファクタリング (refactor)
    - [ ] ドキュメント (docs)

    ## 変更内容

    ### 何を変更したか
    [簡潔な説明]

    ### なぜ変更したか
    [背景・理由]

    ## テスト
    - [ ] ユニットテスト追加
    - [ ] 手動テスト実施

    ## 関連Issue
    Closes #[番号]

## テスト戦略

### テストピラミッド

```
       /\
      /E2E\       少 (遅い、高コスト)
     /------\
    / 統合   \     中
   /----------\
  / ユニット   \   多 (速い、低コスト)
 /--------------\
```

**目標比率**:

- ユニットテスト: 70%
- 統合テスト: 20%
- E2Eテスト: 10%

### テストの書き方

**Given-When-Then パターン**:

```typescript
describe('UserService', () => {
  describe('create', () => {
    it('正常なデータでユーザーを作成できる', async () => {
      // Given: 準備
      const service = new UserService(mockRepository);
      const validData = { name: 'テスト', email: 'test@example.com' };

      // When: 実行
      const result = await service.create(validData);

      // Then: 検証
      expect(result.id).toBeDefined();
      expect(result.name).toBe('テスト');
    });
  });
});
```

### カバレッジ目標

- ビジネスロジック: 90%以上
- 全体: 80%以上
- 100%は目指さない（コストと効果のバランス）

## コードレビュープロセス

### レビューの目的

1. **品質保証**: バグの早期発見
2. **知識共有**: チーム全体でコードベースを理解
3. **学習機会**: ベストプラクティスの共有

### レビューコメントの書き方

**優先度を明示**:

- `[必須]`: 修正必須（セキュリティ、バグ等）
- `[推奨]`: 修正推奨（パフォーマンス、可読性等）
- `[提案]`: 検討してほしい
- `[質問]`: 理解のための質問

**建設的なフィードバック**:

```
// ❌ このコードはダメです。

// ✅ この実装だと O(n²) の時間計算量になります。
//    Mapを使うと O(n) に改善できます。
```

### PRの規模

- 1PR = 1機能
- 変更ファイル数: 10ファイル以内を推奨
- 変更行数: 300行以内を推奨

## 自動化（該当する場合）

### 品質チェックの自動化

プロジェクトの技術スタックに応じて以下を導入してください:

| チェック項目 | 目的 |
| ------------ | ---- |
| Lintチェック | コーディング規約の統一 |
| フォーマッタ | コードスタイルの自動整形 |
| 型チェック | 型安全性の検証 |
| テスト実行 | リグレッション防止 |
| ビルド確認 | デプロイ可能な状態の保証 |

### CI/CDパイプライン

PRの作成時・プッシュ時に自動で品質チェックを実行し、マージ前に品質を担保します。

### Pre-commitフック

コミット前に自動チェックを走らせ、不具合コードの混入を防止します。

## チェックリスト

- [ ] ブランチ戦略が決まっている
- [ ] コミットメッセージ規約が明確である
- [ ] PRテンプレートが用意されている
- [ ] テストの種類とカバレッジ目標が設定されている
- [ ] コードレビュープロセスが定義されている
- [ ] CI/CDパイプラインが構築されている（該当する場合）
